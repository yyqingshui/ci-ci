name: autoMerge

on:
  push:
    branches:
      - master
jobs:
  merge:
    runs-on: ubuntu-latest
    steps:
      - name: auto-merge
        run: |
          curl \
          -X POST \
          -H "Accept: application/vnd.github+json" \
          -H "Authorization: token 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" \
          https://api.github.com/repos/yyqingshui/ci-ci/merges \
          -d '{"base":"develop","head":"master"}'
